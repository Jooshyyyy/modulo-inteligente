<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Item 1: El fondo de borde (Color de Acento) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/bank_accent" />
            <corners
                android:topLeftRadius="12dp"
                android:bottomLeftRadius="12dp"
                android:topRightRadius="12dp"
                android:bottomRightRadius="12dp" />
        </shape>
    </item>

    <!-- Item 2: El cuerpo interior (Fondo Blanco) con offset izquierdo de 5dp para simular un borde izquierdo grueso -->
    <item android:left="5dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/surface_white" />
            <corners
                android:topRightRadius="12dp"
                android:bottomRightRadius="12dp" />
        </shape>
    </item>
</layer-list>
